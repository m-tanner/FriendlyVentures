# Upload only what App Engine needs: the built site, app.yaml, the package
# manifests, and the zero-dependency fallback entrypoint.
*
!app.yaml
!package.json
!package-lock.json
!server.js
!dist
!dist/**
